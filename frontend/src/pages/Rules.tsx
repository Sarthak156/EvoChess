import {useEffect,useState} from 'react';import{api}from'../api/client';export default function Rules(){const[r,setR]=useState<any[]>([]);useEffect(()=>{api.get('/rules').then(setR)},[]);return <main><h1>Rule Library</h1><div className="rules">{r.map(x=><article className="card" key={x.id}><span>#{x.id}</span><h2>{x.name}</h2><p>{x.description}</p><small>Designed for: {x.counters}</small></article>)}</div></main>}
