import {useEffect,useState} from 'react';import {api} from '../api/client';import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';export default function Dashboard(){const[d,setD]=useState<any>();useEffect(()=>{api.get('/dashboard').then(setD)},[]);if(!d)return <main>Loading learning dashboard…</main>;const data=d.reward_curve.map((y:number,x:number)=>({x:x+1,y}));return <main><h1>RL Dashboard</h1><div className="metrics"><div className="card">Episodes <strong>{d.episode_count}</strong></div><div className="card">Average reward <strong>{d.average_reward.toFixed(2)}</strong></div><div className="card">Policy <strong>{d.policy}</strong></div></div><div className="chart card"><h2>Reward curve</h2><ResponsiveContainer width="100%" height={280}><LineChart data={data}><XAxis dataKey="x"/><YAxis/><Tooltip/><Line dataKey="y" stroke="#8b5cf6"/></LineChart></ResponsiveContainer></div><div className="card"><h2>Rule usage</h2>{Object.entries(d.rule_usage).map(([k,v])=><span className="move" key={k}>Rule {k}: {String(v)}</span>)}</div></main>}
